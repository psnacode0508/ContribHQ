{
  "name": "contribhq-root",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "prisma"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build --workspaces --if-present"
  }
}
